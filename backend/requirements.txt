# Minimum bounds so the project installs on current Python (3.11–3.14).
# After a successful install, freeze the resolved set:
#   pip freeze > requirements.lock.txt

# ── Web framework + database + tooling ───────────────────────
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
sqlalchemy>=2.0.36
psycopg[binary]>=3.2
alembic>=1.14
structlog>=24.4
python-dotenv>=1.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.3
httpx>=0.28

# ── Code quality ─────────────────────────────────────────────
ruff>=0.8

# ── Auth ─────────────────────────────────────────────────────
argon2-cffi>=23.1
PyJWT>=2.10

# ── Official-source connectors ───────────────────────────────
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.3
pypdf>=5.1
openpyxl>=3.1

# ── Optional (not required to boot) ──────────────────────────
# slowapi (rate limit)
# playwright (JS-rendered pages only)
# python-dateutil, rapidfuzz
apscheduler>=3.10
# weasyprint (dossier PDF)
# pymupdf, pytesseract, pillow (scanned OCR)
# sentence-transformers, onnxruntime, pgvector (semantic search)
